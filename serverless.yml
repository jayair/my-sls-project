service: serverless-hello-world

provider:
  name: aws
  runtime: nodejs6.10
  environment:
    MESSAGE: "I am ${opt:stage}"

functions:
  helloWorld:
    handler: handler1.helloWorld
    events:
      - http:
          path: /
          method: get
          cors: true

